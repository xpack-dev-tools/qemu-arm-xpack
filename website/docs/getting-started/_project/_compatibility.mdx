
{/* ------------------------------------------------------------------------ */}

## Compatibility

The **xPack QEMU RISC-V** project is fully compatible with the upstream
**QEMU**.

It includes support for `qemu-system-arm` and `qemu-system-aarch64`.
