
{/* ------------------------------------------------------------------------ */}

### Patches

The build uses two patches, one done with `sed` directly in the source tree,
to add `xPack` to the greeting message, and one generated with `git diff`,
to add support for macOS 10.13 (located in the helper `patches` folder).
