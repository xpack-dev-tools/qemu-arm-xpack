
{/* ------------------------------------------------------------------------ */}

### Patches

The build applies patches using `sed` directly in the source tree
to add `xPack` to the greeting message.

The patches that previously added support for macOS 10.13 are no longer used.
